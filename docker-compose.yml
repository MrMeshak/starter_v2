services:
  backend:
    build: .
    ports:
      - 8001:8001
      - 9229:9229 # for debugging
    volumes:
      - .:/usr/src/app
      - /usr/scr/app/.pnpm-store
      - /usr/src/app/node_modules
    command: pnpm start:docker
    environment:
      PORT: 8001
